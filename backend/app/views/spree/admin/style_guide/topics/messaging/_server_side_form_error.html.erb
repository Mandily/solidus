<%- snippet = capture do %>
  <div id="errorExplanation" class="errorExplanation" data-hook="">
    <h2>2 errors prohibited this record from being saved:</h2>
    <p>There were problems with the following fields:</p>
    <ul>
      <li>Email can't be blank</li>
      <li>Password can't be blank</li>
    </ul>
  </div>

  <div class="field withError" id="user_email_field">
    <span class="field_with_errors"><label for="user_email">Email</label></span>
      <span class="field_with_errors"><input class="fullwidth" type="email" value="" name="user[email]" id="user_email"></span>
    <span class="formError">can't be blank</span>
  </div>

<%- end %>

<p>
  Use server side validation to double check data entered after form field validation has cleared.
</p>

<div class="style-guide-result">
  <%= snippet %>
</div>

<div class="style-guide-code">
  <pre><code class="language-html"><%= escape_once snippet %></code></pre>
</div>
